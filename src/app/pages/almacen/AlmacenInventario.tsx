/**
 * ALMACÉN — INVENTARIO
 * Envuelve el componente WarehouseManagement existente
 * y lo integra al sistema de rutas.
 */
import WarehouseManagement from '@/app/WarehouseManagement';

export default function AlmacenInventario() {
  return <WarehouseManagement />;
}
