# ============================================================
# VARIABLES DE ENTORNO - ERP IDP CONSTRUCCIÓN
# Copia este archivo como .env.local para desarrollo
# En Azure Static Web Apps, configura estas en el portal
# ============================================================

# ── MODO DE DATOS ──────────────────────────────────────────
# "mock"  → usa datos en memoria (sin backend, para UI)
# "api"   → conecta al backend FastAPI real
VITE_DATA_MODE=mock

# ── BACKEND API ────────────────────────────────────────────
# URL base del backend FastAPI
# Desarrollo local:
VITE_API_BASE_URL=http://localhost:8000/api/v1
# Azure App Service (producción):
# VITE_API_BASE_URL=https://tu-api.azurewebsites.net/api/v1

# ── APLICACIÓN ─────────────────────────────────────────────
VITE_APP_NAME=ERP IDP Construcción
VITE_APP_VERSION=1.0.0
